import { Typography } from "@material-ui/core";
import React, { ReactElement } from "react";

interface Props {}

export default function RobotPlayer({}: Props): ReactElement {
  return (
    <div>
      <Typography>2nd Player</Typography>
    </div>
  );
}
