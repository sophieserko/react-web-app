import { Button } from "@material-ui/core";
import React, { ReactElement } from "react";

interface Props {}

export default function PaperButton(_props: Props): ReactElement {
  return <Button>Paper</Button>;
}
